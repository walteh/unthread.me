import cache_store from "./cache_store";
import modal_store from "./modal_store";
import session_store from "./session_store";
import token_store from "./token_store";

export default {
	session_store,
	token_store,
	modal_store,
	cache_store,
};
